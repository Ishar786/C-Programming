// Online C compiler to run C program online
#include <stdio.h>

int main() 
{
    int a=0, b=1, total=0, max=0;
    printf(" enter a max value: ");
    scanf("%d", &max);
    if(max==0) 
    {
        printf("%d", a);
    }
    else
    {
        printf("%d\n%d\n", a, b);
    }
    while(total<=max && (a+b)<=max)
    {
        total = a+b;
        printf("%d\n", total);
        a=b;
        b=total;
    }
    printf("\n\n");
    printf("Sheik Ishar");
    return 0;
}
