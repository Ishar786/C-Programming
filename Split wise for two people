// Online C compiler to run C program online
#include <stdio.h>

int main() 
{
    
    int i=0,j=0;
    int ishar=0, tharun=0;
    float expenditure=0, total_1=0, total_2=0;
    printf("enter the expenditure: ");
    scanf("%f", &expenditure);
    printf("who made the expense: ");
    printf("select 1 if ishar is made the payment\n");
    printf("select 2 if tharun made the payment\n");
    printf("enter your choice: ");
    scanf("%d", &i);
    switch (i)
    {
        case 1:
        ishar = expenditure;
        break;
        case 2:
        tharun = expenditure;
        break;
    }
    total_1 = ishar;
    total_2 = tharun;
    printf("ishar's expenditure:%f\n", total_1);
    printf("tharun's expenditure:%f\n", total_2);
    return 0;
}
